mutation UpdateTask(
  $id: String!
  $subjectId: String!
  $academicYearId: String
  $type: TaskType!
  $due_date: String!
  $title: String
  $detail: String
  $completed: Float
) {
  updateTask(
    id: $id
    subjectId: $subjectId
    academicYearId: $academicYearId
    type: $type
    due_date: $due_date
    title: $title
    detail: $detail
    completed: $completed
  )
}
